#include <stdio.h>

int main() {
	int a, b, c, mul, num;
	int arr[1001] = { 0 };
	
	scanf_s("%d \n%d \n%d", &a, &b, &c);
	mul = a * b * c;

	while (mul > 0) {
		num = mul % 10;
		arr[num]++;
		mul = mul / 10;
	}
	for (int i = 0; i < 10; i++) {
		printf("%d\n", arr[i]);
	}
}