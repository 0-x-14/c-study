#include <stdio.h>

int main() {
	int n, x;
	int count = 0;
	scanf_s("%d", &n);
	x = n;

	while (1) {
		int a, b;
		a = n / 10;
		b = n % 10;
		n = b * 10 + (a + b) % 10;
		count++;

		if (n == x) {
			printf("%d", count);
			break;
		}
	}
}