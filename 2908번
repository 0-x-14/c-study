#include <stdio.h>

int main() {

	int a, b;
	int x, y;
	scanf_s("%d %d", &a, &b);

	x = 100 * (a % 10) + (a % 100) - (a % 10) + (a / 100);
	y = 100 * (b % 10) + (b % 100) - (b % 10) + (b / 100);

	if (x > y) {
		printf("%d", x);
	}
	else {
		printf("%d", y);
	}
}