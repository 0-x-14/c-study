#include <stdio.h>

int main() {
	int arr[1000];
	
	for (int i = 0; i < 9; i++) {
		scanf_s("%d", &arr[i]);
	}
	int max = arr[0];
	int num = 1;

	for (int i = 1; i < 9; i++) {
		if (max < arr[i]) {
			max = arr[i];
			num = i + 1;
		}
	}

	printf("%d \n%d", max, num);
}