#include <stdio.h>
#pragma warning (disable : 4996)

int main() {
	int n = 0;
	int count = 0;
	char arr[101];

	scanf_s("%d", &n);

	for (int i = 0; i < n; i++) {
		scanf_s("%s", arr, 100);
	}

	// 수정중
}