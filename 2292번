#include <stdio.h>

int main() {
	int num = 0, plus = 0;
	int a, b;

	scanf_s("%d", &a);

	while (1) {
		num++;
		for (int i = 0; i < num; i++) {
			plus++;
		}
		b = 1 + 6 * plus;
		if (a <= b) {
			printf("%d", num + 1);
			break;
		}
	}

	return 0;
}