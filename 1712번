#include <stdio.h>

int main() {
	int a, b, c, num;
	scanf_s("%d %d %d", &a, &b, &c);

	// (c - b) * num > a;
	num = a / (c - b);
	if (c > b) {
		num += 1;
		printf("%d", num);
	}

	if (c <= b) {
		printf("-1");
	}
}